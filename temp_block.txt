            }

            updateMusicText(on) {
                if (this.musicText) this.musicText.setText(on ? '♪' : '');
            }

            toggleMusic() {
                if (Sfx.Music.isPlaying()) {
                    Sfx.Music.stop();
                    this.updateMusicText(false);
                } else {
                    Sfx.Music.start('calm');
                    this.updateMusicText(true);
                }
            }

            toggleFx() {
                this.fxEnabled = !this.fxEnabled;
                const overlay = document.getElementById('scanlines');
                if (overlay) overlay.style.display = this.fxEnabled ? 'block' : 'none';
